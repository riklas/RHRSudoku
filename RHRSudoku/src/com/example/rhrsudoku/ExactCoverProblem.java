package com.example.rhrsudoku;

import java.util.Set;

public class ExactCoverProblem<E> {
	/*
	 * represents an exact hitting set problem
	 */
	
	Set<E> setX;
	Set<Set<E>> setS;
	
	/*
	 * The problem is to find a subset X* of setX such that each set in setS is
	 * hit by exactly one element in X*
	 * the problem is equivalent to the exact cover problem, and is NP-complete
	 * 
	 * Exact Hitting Set Problem
	 * =========================
	 * Each member of S contains the 
	 */
	
}
